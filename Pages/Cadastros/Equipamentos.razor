@page "/Cadastros/Equipamentos"
@using ProSoft.Data
@using ProSoft.Services
@using ProSoft.Components
@using ProSoft.Pages

<GenericTemplate3 TItem=equipamento Contexto="ctx" HeaderTitle="Equipamentos" Items="ctx.equipamentos.Include(k=>k.Cliente)" ModalSize="modal-lg">
    <GridContent>
        <PropertyColumn TProp=string TGridItem=equipamento Title="Id" Property='p => p.IdEquipamento.ToString("000000")' Sortable="true" />
        <PropertyColumn TProp=string TGridItem=equipamento Title="Descricão" Property="p => p.Descricao" Sortable="true" />
        <TemplateColumn Context="item" TGridItem=equipamento Title="Cliente" >
            @item.Cliente?.Nome
        </TemplateColumn>
    </GridContent>
    <ItemEditForm Context="item">
    </ItemEditForm>
</GenericTemplate3>

@code {
    Contexto ctx = new();
}