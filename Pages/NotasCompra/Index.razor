@page "/NotasCompra/Index"
@using ProSoft.Data
@using Microsoft.EntityFrameworkCore
@using ProSoft.Components

<GenericTemplate3 TItem=notacompra Contexto="ctx" HeaderTitle="Notas de Compra" Items="ctx.notascompra.Include(k=>k.Fornecedor).Include(k=>k.Items).ThenInclude(k=>k.Mercadoria)" ModalSize="modal-xl">
    <GridContent>
        <PropertyColumn TProp=string TGridItem=notacompra Title="Id" Property='p => p.IdNotaCompra.ToString("000000")' Sortable="true" />
        <PropertyColumn TProp=string TGridItem=notacompra Title="Nome" Property="p => p.Fornecedor.Nome" Sortable="true" />
    </GridContent>
    <ItemEditForm Context="item">
        <Form Model="item" Contexto="ctx" />
    </ItemEditForm>
</GenericTemplate3>

@code {
    Contexto ctx = new Contexto();
}
