@page "/OrdemServico/Orcamentos"
@using ProSoft.Data
@using Microsoft.EntityFrameworkCore
@using ProSoft.Components

<GenericTemplate3 TItem=orcamento Contexto="ctx" HeaderTitle="Orçamentos" 
    Items="ctx.orcamentos"
    ModalSize="modal-lg">
    <GridContent>
        <PropertyColumn TProp=string TGridItem=orcamento Title="Id" Property='p => p.IdOrcamento.ToString("000000")' Sortable="true" />
        <PropertyColumn TProp=string TGridItem=orcamento Title="Nome" Property="p => p.Cliente.Nome" Sortable="true" />
    </GridContent>
    <ItemEditForm Context="item">
        <Orcamento Model="item" ctx="ctx" />
    </ItemEditForm>
</GenericTemplate3>

@code {
    Contexto ctx = new Contexto();
}
