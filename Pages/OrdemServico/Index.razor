@page "/OrdemServico/Index"
@using ProSoft.Data
@using Microsoft.EntityFrameworkCore
@using ProSoft.Components

<GenericTemplate3 TItem=ordemdeservico Contexto="ctx" HeaderTitle="Ordens de Serviço" Items="ctx.ordensdeservico" ModalSize="modal-xl">
    <GridContent>
        <PropertyColumn TProp=string TGridItem=ordemdeservico Title="Id" Property='p => p.IdOrdemDeServico.ToString("000000")' Sortable="true" />
        <PropertyColumn TProp=string TGridItem=ordemdeservico Title="Nome" Property="p => p.Data.ToShortDateString()" Sortable="true" />
    </GridContent>
    <ItemEditForm Context="item">
        <OS Model="item" Contexto="ctx" />
    </ItemEditForm>
</GenericTemplate3>

@code {
    Contexto ctx = new();
}
